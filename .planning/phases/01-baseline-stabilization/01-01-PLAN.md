---
phase: 01-baseline-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - README.md
autonomous: true
requirements: [QAV-04]
user_setup: []
must_haves:
  truths:
    - "Clean-install baseline is documented and reproducible."
    - "Baseline verification command runs lint and build together."
    - "Legacy router type package mismatch is removed."
  artifacts:
    - path: package.json
      contains: "\"verify:baseline\": \"npm run lint && npm run build\""
      min_lines: 20
    - path: README.md
      contains: "## Build Baseline Recovery"
      min_lines: 40
  key_links:
    - from: package.json
      to: README.md
      via: "Verification script and documented clean reinstall flow"
      pattern: "verify:baseline|Build Baseline Recovery"
---

<objective>
Resolve dependency and build baseline issues so Phase 1 can execute from a deterministic local environment.

Purpose: eliminate known install/type mismatch blockers and codify a repeatable validation baseline.
Output: aligned dependency metadata, lockfile refresh, and explicit baseline recovery instructions.
</objective>

<execution_context>
@/home/matheus/.codex/get-shit-done/workflows/execute-plan.md
@/home/matheus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-stabilization/01-CONTEXT.md
@.planning/phases/01-baseline-stabilization/01-RESEARCH.md
@package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align dependency baseline and add verification script</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json, .planning/codebase/CONCERNS.md, .planning/phases/01-baseline-stabilization/01-CONTEXT.md</read_first>
  <action>Remove the `@types/react-router-dom` devDependency entry from `package.json`. Add script `"verify:baseline": "npm run lint && npm run build"` to scripts block. Run `npm install` to regenerate `package-lock.json` so the dependency graph reflects the updated package manifest.</action>
  <verify>npm install && npm run verify:baseline</verify>
  <acceptance_criteria>
    - `package.json` does not contain `@types/react-router-dom`
    - `package.json` contains `"verify:baseline": "npm run lint && npm run build"`
    - `package-lock.json` exists and is updated after `npm install`
  </acceptance_criteria>
  <done>Dependency/type mismatch removed and baseline verification command executes successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Document clean reinstall and baseline recovery commands</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/01-baseline-stabilization/01-CONTEXT.md, .planning/phases/01-baseline-stabilization/01-RESEARCH.md</read_first>
  <action>Add a `## Build Baseline Recovery` section to `README.md` with exact commands for deterministic recovery: `rm -rf node_modules package-lock.json`, `npm install`, `npm run verify:baseline`. Include one short note that this is the required Phase 1 baseline flow before marking work done.</action>
  <verify>rg -n "Build Baseline Recovery|npm run verify:baseline|rm -rf node_modules package-lock.json" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains heading `## Build Baseline Recovery`
    - `README.md` contains `npm run verify:baseline`
    - `README.md` contains `rm -rf node_modules package-lock.json`
  </acceptance_criteria>
  <done>Baseline recovery flow is explicitly documented and grep-verifiable in README.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` completes from clean baseline
- [ ] `npm run verify:baseline` succeeds
- [ ] `rg -n "Build Baseline Recovery" README.md` returns one matching section
</verification>

<success_criteria>

- All tasks completed
- `@types/react-router-dom` removed and lockfile refreshed
- Baseline verification script exists and runs lint+build
- Clean reinstall flow is documented in README
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-stabilization/01-01-SUMMARY.md`
</output>
