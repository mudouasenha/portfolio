---
phase: 01-baseline-stabilization
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/legacy/constants-legacy.ts
  - src/constants/index.ts
  - src/components/Projects.tsx
  - src/components/Skills.tsx
  - src/components/Technologies.tsx
autonomous: true
requirements: [QLTY-01, QLTY-04]
user_setup: []
must_haves:
  truths:
    - "Dead/commented runtime blockers are removed from active paths while historical content is preserved outside runtime."
    - "Debug logs are removed from active rendering code."
    - "Animation imports use one approved library pattern across active components."
  artifacts:
    - path: src/legacy/constants-legacy.ts
      contains: "LEGACY_CONSTANTS_NOT_IN_USE"
      min_lines: 10
    - path: src/constants/index.ts
      contains: "Deprecated runtime constants module"
      min_lines: 3
    - path: src/components/Projects.tsx
      contains: "const projects ="
      min_lines: 30
  key_links:
    - from: src/components/Skills.tsx
      to: src/components/Technologies.tsx
      via: "single motion import strategy"
      pattern: "from \"motion/react\""
    - from: src/constants/index.ts
      to: src/legacy/constants-legacy.ts
      via: "legacy extraction"
      pattern: "legacy|Deprecated runtime constants module"
---

<objective>
Remove debug and dead-code blockers in active paths while preserving historical content for reference.

Purpose: reduce maintenance friction and eliminate stale runtime behaviors before deeper migration.
Output: cleaned runtime modules, isolated legacy snapshot, and consolidated motion import usage.
</objective>

<execution_context>
@/home/matheus/.codex/get-shit-done/workflows/execute-plan.md
@/home/matheus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-stabilization/01-CONTEXT.md
@.planning/phases/01-baseline-stabilization/01-RESEARCH.md
@src/constants/index.ts
@src/components/Projects.tsx
@src/components/Skills.tsx
@src/components/Technologies.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve legacy constant text outside active runtime</name>
  <files>src/legacy/constants-legacy.ts, src/constants/index.ts</files>
  <read_first>src/constants/index.ts, .planning/phases/01-baseline-stabilization/01-CONTEXT.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Create `src/legacy/constants-legacy.ts` and move preserved historical content there as a non-runtime export `LEGACY_CONSTANTS_NOT_IN_USE`. Rewrite `src/constants/index.ts` into a trimmed placeholder module containing a short deprecation note (`Deprecated runtime constants module`) and no module-level translation calls (`t(...)`). Keep this module import-safe and minimal.</action>
  <verify>rg -n "LEGACY_CONSTANTS_NOT_IN_USE|Deprecated runtime constants module|t\\(" src/legacy/constants-legacy.ts src/constants/index.ts</verify>
  <acceptance_criteria>
    - `src/legacy/constants-legacy.ts` exists and contains `LEGACY_CONSTANTS_NOT_IN_USE`
    - `src/constants/index.ts` contains `Deprecated runtime constants module`
    - `src/constants/index.ts` does not contain module-level `t(` usage
  </acceptance_criteria>
  <done>Historical text is preserved in legacy path and active constants module is trimmed and runtime-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Remove debug logs from active rendering paths</name>
  <files>src/components/Projects.tsx</files>
  <read_first>src/components/Projects.tsx, .planning/phases/01-baseline-stabilization/01-CONTEXT.md</read_first>
  <action>Delete `console.log('rawProjects:', projects);` and any additional render-path debug logs from `src/components/Projects.tsx`. Keep the projects mapping and rendering behavior unchanged except for debug-output removal.</action>
  <verify>rg -n "console\\.log\\(" src/components/Projects.tsx</verify>
  <acceptance_criteria>
    - `src/components/Projects.tsx` does not contain `console.log(`
    - Projects list mapping and rendering block remains present
  </acceptance_criteria>
  <done>Projects component is free of active debug logging without functional regression.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate motion imports to approved runtime package</name>
  <files>src/components/Skills.tsx, src/components/Technologies.tsx</files>
  <read_first>src/components/Skills.tsx, src/components/Technologies.tsx, .planning/phases/01-baseline-stabilization/01-CONTEXT.md</read_first>
  <action>Replace `framer-motion` imports with `motion/react` in `Skills.tsx` and `Technologies.tsx`. Keep existing animation behavior and variants API usage intact by using `motion` and `Variants` imports supported by the approved package strategy. Ensure no `framer-motion` imports remain anywhere in `src/`.</action>
  <verify>rg -n "framer-motion|from \\\"motion/react\\\"" src/components/Skills.tsx src/components/Technologies.tsx && rg -n "framer-motion" src</verify>
  <acceptance_criteria>
    - `src/components/Skills.tsx` imports from `"motion/react"`
    - `src/components/Technologies.tsx` imports from `"motion/react"`
    - `rg -n "framer-motion" src` returns no matches
  </acceptance_criteria>
  <done>Animation import strategy is consolidated and grep-verifiable across active source files.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] `rg -n "console\\.log\\(" src` only returns intentionally retained non-runtime references (if any)
- [ ] `rg -n "framer-motion" src` returns no matches
</verification>

<success_criteria>

- All tasks completed
- Runtime debug logs removed from active component paths
- Legacy text preserved outside active runtime in dedicated `src/legacy/` file
- Motion imports unified to approved package usage in active source
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-stabilization/01-03-SUMMARY.md`
</output>
