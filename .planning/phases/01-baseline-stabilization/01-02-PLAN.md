---
phase: 01-baseline-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/MainRoutes.tsx
  - src/LangRouter.tsx
  - src/i18n.tsx
  - src/components/LanguageSwitcher.tsx
  - src/features/i18n/detectPreferredLanguage.ts
autonomous: true
requirements: [I18N-01]
user_setup: []
must_haves:
  truths:
    - "Language routing is URL-authoritative after route resolution."
    - "Root path performs one-time language detection and redirects to a language-prefixed route."
    - "Invalid language routes redirect to /en and language switch updates both route and rendered content."
  artifacts:
    - path: src/features/i18n/detectPreferredLanguage.ts
      contains: "export function detectPreferredLanguage"
      min_lines: 10
    - path: src/MainRoutes.tsx
      contains: "detectPreferredLanguage"
      min_lines: 15
    - path: src/LangRouter.tsx
      contains: "Loading language..."
      min_lines: 20
  key_links:
    - from: src/MainRoutes.tsx
      to: src/features/i18n/detectPreferredLanguage.ts
      via: "root redirect language resolution"
      pattern: "Navigate to=\\{`/\\$\\{detectPreferredLanguage\\(\\)\\}`\\}"
    - from: src/components/LanguageSwitcher.tsx
      to: src/LangRouter.tsx
      via: "route language segment as source of truth"
      pattern: "localStorage\\.setItem\\('portfolio\\.lang', langCode\\)|navigate\\("
---

<objective>
Normalize routing and language source-of-truth behavior using the locked hybrid contract.

Purpose: preserve multilingual continuity while eliminating competing language authorities.
Output: deterministic root detection, route-authoritative language sync, and explicit fallback behavior.
</objective>

<execution_context>
@/home/matheus/.codex/get-shit-done/workflows/execute-plan.md
@/home/matheus/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-stabilization/01-CONTEXT.md
@.planning/phases/01-baseline-stabilization/01-RESEARCH.md
@src/MainRoutes.tsx
@src/LangRouter.tsx
@src/i18n.tsx
@src/components/LanguageSwitcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one-time root language detection utility and route redirect integration</name>
  <files>src/features/i18n/detectPreferredLanguage.ts, src/MainRoutes.tsx</files>
  <read_first>src/MainRoutes.tsx, src/components/LanguageSwitcher.tsx, .planning/phases/01-baseline-stabilization/01-CONTEXT.md</read_first>
  <action>Create `src/features/i18n/detectPreferredLanguage.ts` with `detectPreferredLanguage(): 'en' | 'pt'` that checks `localStorage.getItem('portfolio.lang')`, then `navigator.language`, then falls back to `en`. Update `/` route in `MainRoutes.tsx` to redirect with `Navigate` to ``/${detectPreferredLanguage()}`` instead of hardcoded `/en`.</action>
  <verify>rg -n "detectPreferredLanguage|portfolio\\.lang|Navigate to=\\{`/\\$\\{detectPreferredLanguage\\(\\)\\}`\\}" src/MainRoutes.tsx src/features/i18n/detectPreferredLanguage.ts</verify>
  <acceptance_criteria>
    - `src/features/i18n/detectPreferredLanguage.ts` exports `detectPreferredLanguage`
    - `src/MainRoutes.tsx` imports and uses `detectPreferredLanguage()` for `/` redirect
    - `src/MainRoutes.tsx` still contains wildcard fallback redirect to `/en`
  </acceptance_criteria>
  <done>Root route follows hybrid contract and no longer hardcodes `/en` blindly.</done>
</task>

<task type="auto">
  <name>Task 2: Align i18n bootstrap and LangRouter with URL-authoritative contract</name>
  <files>src/i18n.tsx, src/LangRouter.tsx</files>
  <read_first>src/i18n.tsx, src/LangRouter.tsx, .planning/codebase/CONCERNS.md</read_first>
  <action>Remove active `LanguageDetector` usage from `src/i18n.tsx` and configure `supportedLngs: ['en', 'pt']` with `fallbackLng: 'en'` without hardcoded `lng`. In `src/LangRouter.tsx`, keep invalid lang redirect to `/en...` and replace blank `return null` waiting state with a minimal accessible fallback element containing text `Loading language...` and `aria-live=\"polite\"`.</action>
  <verify>rg -n "supportedLngs|fallbackLng|Loading language|aria-live|Navigate to=\\{`/en" src/i18n.tsx src/LangRouter.tsx</verify>
  <acceptance_criteria>
    - `src/i18n.tsx` contains `supportedLngs: ['en', 'pt']`
    - `src/i18n.tsx` does not use `LanguageDetector` plugin
    - `src/LangRouter.tsx` contains `Loading language...` and `aria-live="polite"`
  </acceptance_criteria>
  <done>Route-authoritative language sync is explicit and loading state is no longer a blank frame.</done>
</task>

<task type="auto">
  <name>Task 3: Persist language preference from LanguageSwitcher</name>
  <files>src/components/LanguageSwitcher.tsx</files>
  <read_first>src/components/LanguageSwitcher.tsx, src/features/i18n/detectPreferredLanguage.ts, .planning/phases/01-baseline-stabilization/01-CONTEXT.md</read_first>
  <action>In `handleLanguageChange`, write `localStorage.setItem('portfolio.lang', langCode)` before navigation. Keep URL-segment replacement logic and `i18n.changeLanguage(langCode)` behavior so route and rendered language remain synchronized.</action>
  <verify>rg -n "localStorage\\.setItem\\('portfolio\\.lang', langCode\\)|i18n\\.changeLanguage\\(langCode\\)|navigate\\(" src/components/LanguageSwitcher.tsx</verify>
  <acceptance_criteria>
    - `src/components/LanguageSwitcher.tsx` contains `localStorage.setItem('portfolio.lang', langCode)`
    - URL replacement logic still updates the first path segment language code
    - `i18n.changeLanguage(langCode)` call is preserved
  </acceptance_criteria>
  <done>Language preference is persisted and route-driven behavior remains intact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manual smoke: `/` redirects to detected language route
- [ ] Manual smoke: invalid route `/es` redirects to `/en`
- [ ] Manual smoke: language switch updates URL and visible language
</verification>

<success_criteria>

- All tasks completed
- Hybrid language contract is implemented exactly as approved
- Invalid language handling remains deterministic (`/en` fallback)
- Loading state in LangRouter is explicit and accessible
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-stabilization/01-02-SUMMARY.md`
</output>
